╔═══════════════════════════════════════════════════════════════════════╗
║           YOLO DATASET STRUCTURE TRANSFORMATION                       ║
║           Before → After reorganize_to_yolo_structure.py              ║
╚═══════════════════════════════════════════════════════════════════════╝

═══════════════════════════════════════════════════════════════════════
STEP 1: INITIAL STATE (After CSV conversion)
═══════════════════════════════════════════════════════════════════════

general_dataset/
├── train/                    ← Images mixed with labels ❌
│   ├── IMG_001.JPG
│   ├── IMG_002.JPG
│   ├── ... (928 images)
│   └── labels/               ← Labels nested in images dir ❌
│       ├── IMG_001.txt
│       ├── IMG_002.txt
│       └── ... (928 labels)
├── val/
│   ├── IMG_100.JPG
│   ├── ... (111 images)
│   └── labels/
│       ├── IMG_100.txt
│       └── ... (111 labels)
├── test/
│   ├── IMG_200.JPG
│   ├── ... (258 images)
│   └── labels/
│       ├── IMG_200.txt
│       └── ... (258 labels)
└── groundtruth/
    └── csv/
        ├── train_big_size_A_B_E_K_WH_WB.csv
        ├── val_big_size_A_B_E_K_WH_WB.csv
        └── test_big_size_A_B_E_K_WH_WB.csv

PROBLEM:
  • train_vm.py expects: IMAGES_TRAIN = DATASET_ROOT / "train"
  • This points to: general_dataset/train/
  • Which exists, but is the OLD structure!
  • After reorganization, images are at: general_dataset/images/train/

═══════════════════════════════════════════════════════════════════════
STEP 2: REORGANIZATION (python reorganize_to_yolo_structure.py)
═══════════════════════════════════════════════════════════════════════

ACTIONS PERFORMED:

1. Create new directories:
   ✓ general_dataset/images/train/
   ✓ general_dataset/images/val/
   ✓ general_dataset/images/test/
   ✓ general_dataset/labels/train/
   ✓ general_dataset/labels/val/
   ✓ general_dataset/labels/test/

2. Move images:
   • general_dataset/train/*.JPG → general_dataset/images/train/
   • general_dataset/val/*.JPG   → general_dataset/images/val/
   • general_dataset/test/*.JPG  → general_dataset/images/test/

3. Move labels:
   • general_dataset/train/labels/*.txt → general_dataset/labels/train/
   • general_dataset/val/labels/*.txt   → general_dataset/labels/val/
   • general_dataset/test/labels/*.txt  → general_dataset/labels/test/

4. Clean up:
   • Remove empty general_dataset/train/labels/
   • Remove empty general_dataset/val/labels/
   • Remove empty general_dataset/test/labels/

═══════════════════════════════════════════════════════════════════════
STEP 3: FINAL STATE (YOLO Standard Structure)
═══════════════════════════════════════════════════════════════════════

general_dataset/
├── images/                   ← All images here ✅
│   ├── train/
│   │   ├── IMG_001.JPG
│   │   ├── IMG_002.JPG
│   │   └── ... (928 images)
│   ├── val/
│   │   ├── IMG_100.JPG
│   │   └── ... (111 images)
│   └── test/
│       ├── IMG_200.JPG
│       └── ... (258 images)
├── labels/                   ← All labels here ✅
│   ├── train/
│   │   ├── IMG_001.txt
│   │   ├── IMG_002.txt
│   │   └── ... (928 labels)
│   ├── val/
│   │   ├── IMG_100.txt
│   │   └── ... (111 labels)
│   └── test/
│       ├── IMG_200.txt
│       └── ... (258 labels)
└── groundtruth/              ← Original CSVs (unchanged) ✅
    └── csv/
        ├── train_big_size_A_B_E_K_WH_WB.csv
        ├── val_big_size_A_B_E_K_WH_WB.csv
        └── test_big_size_A_B_E_K_WH_WB.csv

RESULT:
  • Clean separation: images/ and labels/
  • Standard YOLO structure ✅
  • train_vm.py now points to: DATASET_ROOT / "images" / "train"
  • Which correctly points to: general_dataset/images/train/

═══════════════════════════════════════════════════════════════════════
CONFIGURATION CHANGES IN train_vm.py
═══════════════════════════════════════════════════════════════════════

BEFORE (Lines 41-46):
┌───────────────────────────────────────────────────────────────────┐
│ IMAGES_TRAIN = DATASET_ROOT / "train"                             │
│ IMAGES_VAL = DATASET_ROOT / "val"                                 │
│ IMAGES_TEST = DATASET_ROOT / "test"                               │
└───────────────────────────────────────────────────────────────────┘
  Points to: .../general_dataset/train/
  Status: ❌ OLD STRUCTURE (doesn't exist after reorganization)

AFTER (Lines 41-52):
┌───────────────────────────────────────────────────────────────────┐
│ IMAGES_TRAIN = DATASET_ROOT / "images" / "train"                  │
│ IMAGES_VAL = DATASET_ROOT / "images" / "val"                      │
│ IMAGES_TEST = DATASET_ROOT / "images" / "test"                    │
│                                                                    │
│ LABELS_TRAIN = DATASET_ROOT / "labels" / "train"                  │
│ LABELS_VAL = DATASET_ROOT / "labels" / "val"                      │
│ LABELS_TEST = DATASET_ROOT / "labels" / "test"                    │
└───────────────────────────────────────────────────────────────────┘
  Points to: .../general_dataset/images/train/
  Status: ✅ NEW STRUCTURE (correct!)

═══════════════════════════════════════════════════════════════════════
CLASS NAMES FIX
═══════════════════════════════════════════════════════════════════════

BEFORE (Lines 49-56):
┌───────────────────────────────────────────────────────────────────┐
│ CLASS_NAMES = {                                                    │
│     1: "Buffalo",     ❌ YOLO needs 0-indexed!                    │
│     2: "Elephant",                                                 │
│     3: "Kudu",                                                     │
│     4: "Topi",                                                     │
│     5: "Warthog",                                                  │
│     6: "Waterbuck"                                                 │
│ }                                                                  │
└───────────────────────────────────────────────────────────────────┘

AFTER (Lines 54-61):
┌───────────────────────────────────────────────────────────────────┐
│ CLASS_NAMES = {                                                    │
│     0: "Buffalo",     ✅ Correct YOLO format!                     │
│     1: "Elephant",                                                 │
│     2: "Kudu",                                                     │
│     3: "Topi",                                                     │
│     4: "Warthog",                                                  │
│     5: "Waterbuck"                                                 │
│ }                                                                  │
└───────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════
MODEL FIX
═══════════════════════════════════════════════════════════════════════

BEFORE (Line 59):
┌───────────────────────────────────────────────────────────────────┐
│ MODEL = "yolo11x-obb.pt"  ❌ Oriented bounding boxes              │
└───────────────────────────────────────────────────────────────────┘

AFTER (Line 64):
┌───────────────────────────────────────────────────────────────────┐
│ MODEL = "yolo11s.pt"  ✅ Regular bounding boxes                   │
└───────────────────────────────────────────────────────────────────┘

WHY?
  • Your dataset uses REGULAR bounding boxes (horizontal rectangles)
  • OBB models are for ORIENTED bounding boxes (rotated rectangles)
  • Using wrong model = training failure

AVAILABLE MODELS:
  yolo11n.pt  - Nano   (2.5M params, fastest)
  yolo11s.pt  - Small  (9.4M params, good balance) ⭐ DEFAULT
  yolo11m.pt  - Medium (20M params, better accuracy)
  yolo11l.pt  - Large  (25M params, high accuracy)
  yolo11x.pt  - Extra  (56M params, best accuracy)

═══════════════════════════════════════════════════════════════════════
DATA.YAML GENERATION
═══════════════════════════════════════════════════════════════════════

BEFORE:
┌───────────────────────────────────────────────────────────────────┐
│ path: /path/to/general_dataset                                     │
│ train: train          ❌ Points to old structure                  │
│ val: val                                                           │
│ test: test                                                         │
│ nc: 6                                                              │
│ names: [Buffalo, Elephant, Kudu, Topi, Warthog, Waterbuck]       │
└───────────────────────────────────────────────────────────────────┘

AFTER:
┌───────────────────────────────────────────────────────────────────┐
│ path: /path/to/general_dataset                                     │
│ train: images/train   ✅ Points to new structure                  │
│ val: images/val                                                    │
│ test: images/test                                                  │
│ nc: 6                                                              │
│ names: [Buffalo, Elephant, Kudu, Topi, Warthog, Waterbuck]       │
└───────────────────────────────────────────────────────────────────┘

YOLO WILL LOOK FOR:
  Images: {path}/{train}  = general_dataset/images/train/*.JPG ✅
  Labels: {path}/labels/{train_basename}  = general_dataset/labels/train/*.txt ✅

═══════════════════════════════════════════════════════════════════════
COMPLETE WORKFLOW
═══════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────┐
│ 1️⃣  SETUP (First time)                                          │
└─────────────────────────────────────────────────────────────────┘
  cd /home/estudiante/grupo_12/subsaharian_dataset/yolo_vm
  conda env create -f environment.yml
  conda activate yolov11-wildlife

┌─────────────────────────────────────────────────────────────────┐
│ 2️⃣  CONVERT CSV → YOLO LABELS (First time)                     │
└─────────────────────────────────────────────────────────────────┘
  python convert_csv_to_yolo.py
  
  Creates:
    train/*.JPG + train/labels/*.txt
    val/*.JPG + val/labels/*.txt
    test/*.JPG + test/labels/*.txt

┌─────────────────────────────────────────────────────────────────┐
│ 3️⃣  REORGANIZE STRUCTURE (First time)                          │
└─────────────────────────────────────────────────────────────────┘
  python reorganize_to_yolo_structure.py
  
  Transforms:
    train/*.JPG → images/train/*.JPG
    train/labels/*.txt → labels/train/*.txt

┌─────────────────────────────────────────────────────────────────┐
│ 4️⃣  TRAIN (Every time)                                         │
└─────────────────────────────────────────────────────────────────┘
  python train_vm.py --epochs 50 --batch 4
  
  Now works because:
    • Paths point to images/train/ ✅
    • Class IDs are 0-indexed ✅
    • Model type is correct ✅

═══════════════════════════════════════════════════════════════════════
VERIFICATION COMMANDS
═══════════════════════════════════════════════════════════════════════

# Check structure exists
ls -la ../general_dataset/
# Output should show: images/ labels/ groundtruth/

# Check images
ls ../general_dataset/images/train/*.JPG | wc -l
# Output: 928

# Check labels
ls ../general_dataset/labels/train/*.txt | wc -l
# Output: 928

# Verify they match
diff <(ls ../general_dataset/images/train/ | sed 's/.JPG//') \
     <(ls ../general_dataset/labels/train/ | sed 's/.txt//')
# Output: (no output = perfect match!) ✅

# Check label format
head -n 1 ../general_dataset/labels/train/IMG_001.txt
# Output: 0 0.5234 0.6123 0.1234 0.0987
#         ↑ class_id (0-5) ✅

═══════════════════════════════════════════════════════════════════════
TROUBLESHOOTING
═══════════════════════════════════════════════════════════════════════

ERROR: "Training images not found at .../train"
CAUSE: Script still using old paths
FIX: Changes already applied to train_vm.py ✅

ERROR: "Training labels not found"
CAUSE: Labels not created or not reorganized
FIX: 
  python convert_csv_to_yolo.py
  python reorganize_to_yolo_structure.py

ERROR: "Wrong class detected"
CAUSE: Class IDs not 0-indexed
FIX: Already fixed in train_vm.py ✅

ERROR: Model training fails immediately
CAUSE: Wrong model type (OBB vs regular)
FIX: Already fixed in train_vm.py ✅

═══════════════════════════════════════════════════════════════════════
STATUS
═══════════════════════════════════════════════════════════════════════

✅ train_vm.py updated with correct paths
✅ Class IDs fixed (0-5 instead of 1-6)
✅ Model type fixed (yolo11s.pt instead of yolo11x-obb.pt)
✅ Error messages improved with solutions
✅ Documentation created

READY TO USE! 🚀

═══════════════════════════════════════════════════════════════════════
LAST UPDATED: 2025-11-20
═══════════════════════════════════════════════════════════════════════

